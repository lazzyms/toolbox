# Sign PDF

Launch Toolbox, then choose `All tools` → `PDF` → `Sign PDF`. Enter a typed signature, select a page, and run it. Verify a visible non-cryptographic signature overlay and unchanged original.
