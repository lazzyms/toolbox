# Extract GIF Frames

Launch Toolbox, then choose `All tools` → `Images` → `Extract GIF Frames`. Select an animated GIF and run it. Verify one output per frame and no flattening of the source animation.
