# Image Tone

Choose `Tone`, select an image, adjust brightness, saturation, or exposure, and
run it. Verify pixel values change as requested, the output is a collision-safe
`-tone` copy, and the source remains unchanged.
