# Edit PDF

Choose `Edit`, select a PDF, choose Text, Note, Highlight, or Shape, and run
it. For text, note, or highlight edits, enter the annotation text; optionally
enter a comma-separated page list to limit the edit. Verify the output contains
the requested annotation, preserves page geometry, uses a collision-safe
`-edited.pdf` path, and leaves the source unchanged.
