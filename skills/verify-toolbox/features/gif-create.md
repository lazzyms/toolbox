# Create GIF

Launch Toolbox, then choose `All tools` → `Images` → `Create GIF`. Select multiple same-sized images and run it. Verify an animated GIF with the expected frame count and unchanged inputs.
