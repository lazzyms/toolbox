# Image Metadata

Launch Toolbox, then choose `All tools` → `Images` → `Image Metadata`. Select an image and run it. Verify the output follows the privacy policy and the original metadata and bytes remain untouched.
