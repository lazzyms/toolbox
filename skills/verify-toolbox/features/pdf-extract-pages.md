# Extract PDF Pages

Launch Toolbox, then choose `All tools` → `PDF` → `Extract PDF Pages`. Select a page range and run it. Verify only selected pages remain in order and the output is collision-safe.
