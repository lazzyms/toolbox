# Image Watermark

Launch Toolbox, then choose `All tools` → `Images` → `Watermark Images`. Select an image, set opacity, and run it. Verify a visibly changed output, requested opacity behavior, and unchanged source.
