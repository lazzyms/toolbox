# Watermark PDF

Launch Toolbox, then choose `All tools` → `PDF` → `Watermark PDF`. Select a PDF, enter text, and run it. Verify a `-watermarked.pdf` has a visible overlay and the source is unchanged.
