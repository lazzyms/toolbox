# PDF Page Numbers

Choose `Page Numbers`, select a PDF, and run it. Verify a collision-safe `-numbered.pdf` preserves page count, dimensions, and original bytes while adding a visible number overlay.
