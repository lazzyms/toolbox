# PDF Page Numbers

Launch Toolbox, then choose `All tools` → `PDF` → `Add PDF Page Numbers`. Select a PDF and run it. Verify a collision-safe `-numbered.pdf` preserves page count, dimensions, and original bytes while adding a visible number overlay.
