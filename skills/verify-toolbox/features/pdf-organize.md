# Organize PDF

Choose `Organize`, reorder thumbnails, rotate or delete pages, or switch the editor to `Add blank pages`. Verify the typed plan is reflected in output order and page count, and verify inserted pages use the selected page size. The original PDF must remain unchanged.
