# Crop PDF

Launch Toolbox, then choose `All tools` → `PDF` → `Crop PDF`. Inspect a PDF, select a rectangle and scope, and run it. Verify the output MediaBox matches the plan, remains readable, and never overwrites the input.
