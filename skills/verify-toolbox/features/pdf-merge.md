# Merge PDF

Launch Toolbox, then choose `All tools` → `PDF` → `Merge PDF`. Select two PDFs and run it. Verify qpdf produces one `-merged.pdf` with input pages in order and leaves both inputs unchanged.
