# Blur Faces

Choose `All tools` → `Images` → `Blur Faces`. In the current build this tool is explicitly unavailable: verify the unavailable message, the absence of a file picker/run control, and that no output is created. If an offline adapter is supplied in a future build, verify that it produces `-blurred.png`; never accept a fake output when the adapter is missing.
