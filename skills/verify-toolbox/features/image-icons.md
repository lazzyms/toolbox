# App Icons

Launch Toolbox, then choose `All tools` → `Images` → `Generate App Icons`. Select an image and run it. Verify every configured preset is emitted as a PNG with expected dimensions and transparent padding policy.
