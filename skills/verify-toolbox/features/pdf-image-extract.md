# Extract PDF Images

Choose `Extract Images`, select a PDF containing an embedded JPEG, and run it. Verify the extracted bytes and dimensions match the embedded image; unsupported filters fail explicitly.
