# Extract PDF Images

Launch Toolbox, then choose `All tools` → `PDF` → `Extract Images from PDF`. Select a PDF containing an embedded JPEG and run it. Verify the extracted bytes and dimensions match the embedded image; unsupported filters fail explicitly.
