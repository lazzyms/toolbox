# PDF to Images

Launch Toolbox, then choose `All tools` → `PDF` → `PDF to Images`. Select a PDF and run it. Verify `pdftoppm` produces one JPEG per page at the requested DPI and reports a clear error when unavailable.
